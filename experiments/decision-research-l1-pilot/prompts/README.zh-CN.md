# 统一提示词库

这里是实验任务和实际用户提示的唯一来源。两个隔离工作区不再保存 `TASK.md`、`START-HERE.md` 或任务副本。

每个文件应记录：

- task ID 与开发/holdout 状态；
- 研究任务正文；
- 实际发送的 user prompt；
- 调用方式：默认、显式 Skill 或自然语言隐式触发；
- 预定输出路径。

运行时只复制对应文件标出的 user prompt，不要把本目录或其他条件信息提供给执行对话。
